import 'package:flutter_test/flutter_test.dart';
import 'package:coil/main.dart';

void main() {
  testWidgets('IterApp basic widget test', (WidgetTester tester) async {
    expect(const IterApp(), isNotNull);
  });
}
