org.gradle.jvmargs=-Xmx3584m -XX:MaxMetaspaceSize=1024m -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.vfs.watch=false
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
# Windows cross-drive workaround: Kotlin incremental compilation fails when
# the Pub cache (C:\) and project build dir (D:\) are on different drives.
kotlin.incremental=false
# Dev environment configuration
APP_ENV=dev

